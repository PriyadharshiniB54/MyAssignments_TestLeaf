package week3.day2.homeassignments;

/*Create an interface DatabseConnection with the following abstract methods: - connect() - disconnect() - executeUpdate()
*/

public interface DatabseConnection {
	
	void connect();
	void disconnect();
	void executeUpdate();
	
}
